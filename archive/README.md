# Archived benchmark material

[benchmarks-legacy/](benchmarks-legacy/) preserves the former case collection
and its tools for possible future reuse. The active paper benchmark is
[../benchmarks/](../benchmarks/). Archives are outside the active runner's
input inventory and coverage denominator.
